services:
  app:
    build: .
    # No `user:` needed — bin/docker-entrypoint adopts the ownership of the
    # bind-mounted project, so the container writes files as the host user.
    volumes:
      - .:/app
    tty: true
