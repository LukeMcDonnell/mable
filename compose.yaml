services:
  app:
    build: .
    # Run as the host user so files written by the container (generated code,
    # gems, the SQLite db) stay owned by you rather than root.
    user: "${HOST_UID:-1000}:${HOST_GID:-1000}"
    volumes:
      - .:/app
    tty: true
